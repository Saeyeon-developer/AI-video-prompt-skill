# Sources and adaptations

Reviewed against the user-supplied snapshot on 2026-09-05:

| Historical authoring source (not distributed) | Retained guidance |
| --- | --- |
| `Base-H3/SKILL.md` | T2VA/I2VA/FL2VA/L2VA and Ref2VA routing |
| `Base-H3/base-en.txt` | Three fields, exact frame-alignment syntax, shot/cut notation, speakers, dialogue, sound |
| `Base-H3/ref-en.txt` | Six fields, subject/frame/video/audio roles, retention markers, audio copying and speaker numbering |

The source files are described by the repository owner as official H3 materials. Their upstream URL, revision, and license are not included in the supplied files; no independent upstream verification is claimed. These source paths are provenance records, not runtime dependencies. The installed folder is self-contained.

The two original reference copies were reorganized into base modes, shared syntax, and full-reference guidance. Long source examples were replaced by compact authored examples; the source example with dialogue continuing across a cut without the required paired markers is not carried forward. Exact field names, relationship markers, and mode-specific alignment notation remain intact. The source's 4–15-second statement is scoped to that source rather than advertised as a universal API limit. Language and presentation are defaults that yield to user requests.

Codex authoring follows [OpenAI model guidance](https://developers.openai.com/api/docs/guides/latest-model) and [Build skills](https://learn.chatgpt.com/docs/build-skills): prioritize the user's intended result, avoid conflicting process rules, and disclose specialized detail only when relevant. These are assistant-authoring principles, not H3 API specifications.

This is a community adaptation, not an official MiniMax release. Attribution records do not themselves grant a redistribution license; the repository maintainer supplies release licensing separately.
